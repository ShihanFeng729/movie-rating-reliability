# Tests

This directory will contain automated tests for the Python package.

Tests should use synthetic fixtures whenever possible so they do not require
licensed datasets, live API calls, or a personal API key.
